import assert from "assert";

import { db } from "#databases/databases";

import { client } from "#test/utils/httpClient";
import { genUsers, User } from "#test/utils/genUser";
import { insertSegment, insertVip } from "#test/utils/queryGen";

const endpoint = "/api/endpoint";

const postTestEndpoint = () => client({
    method: "POST",
    url: endpoint,
    data: {
    }
});

const cases = [
    "firstCase",
    "secondCase",
    "thirdCase"
];
const users = genUsers("endpoint", cases);
const vipUser = genUser("endpoint", "vip");
