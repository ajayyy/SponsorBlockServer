name: create-sqlite-base
on:
  push:
    paths:
      - databases/**

jobs:
  uses: ajayyy/SponsorBlockServer/.github/workflows/node-setup.yml@master
  make-base-db:
    runs-on: ubuntu-latest
    steps:
      - name: Set config
        run: |
          echo '{"mode": "init-db-and-exit"}' > config.json
      - name: Run Server
        timeout-minutes: 10
        run: npm start
      - uses: actions/upload-artifact@v2
        with:
          name: SponsorTimesDB.db
          path: databases/sponsorTimes.db